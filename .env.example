# Optional: copy to `.env` at the repo root if you run Prisma CLI from here.
# The API normally loads `apps/api/.env` via dotenv.
DATABASE_URL="postgresql://marketplace:marketplace@localhost:5432/marketplace"
